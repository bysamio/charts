{{- if and .Values.auth.enabled (not .Values.auth.existingPasswordSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "memcached.secretName" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "memcached.labels" . | nindent 4 }}
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  {{- if .Values.auth.username }}
  memcached-username: {{ .Values.auth.username | b64enc | quote }}
  {{- end }}
  {{- if .Values.auth.password }}
  memcached-password: {{ .Values.auth.password | b64enc | quote }}
  {{- else }}
  memcached-password: {{ randAlphaNum 10 | b64enc | quote }}
  {{- end }}
{{- end }}
