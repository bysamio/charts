apiVersion: v1
kind: Service
metadata:
  name: {{ include "casepack-api.fullname" . }}
  labels:
    {{- include "casepack-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "casepack-api.selectorLabels" . | nindent 4 }}
