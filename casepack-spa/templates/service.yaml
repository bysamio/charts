apiVersion: v1
kind: Service
metadata:
  name: {{ include "casepack-spa.fullname" . }}
  labels:
    {{- include "casepack-spa.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "casepack-spa.selectorLabels" . | nindent 4 }}
